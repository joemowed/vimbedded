{
    "name": "vimbedded-gui",
    "version": "1.0.0",
    "description": "platformio reimagined for linux based embedded development with nvim.",
    "main": "electron/build/main.js",
    "scripts": {
        "init-script": "npm install && ./utility/SUIDSandbox.sh",
        "buildElectron": "cd electron && npx tsc -b",
        "devElectron": "npm run-script buildElectron &&  electron .",
        "devAll": "npm run-script buildElectron && npm --prefix gui-angular run-script build && electron ."
    },
    "author": "Joe Maloney",
    "license": "GPL-3.0",
    "dependencies": {
        "electron": "^31.1.0"
    },
    "devDependencies": {
        "@types/node": "^20.14.10",
        "concurrently": "^8.2.2",
        "typescript": "^5.5.3"
    }
}
